import { ProductNameDirective } from './product-name.directive';

describe('ProductNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductNameDirective();
    expect(directive).toBeTruthy();
  });
});
