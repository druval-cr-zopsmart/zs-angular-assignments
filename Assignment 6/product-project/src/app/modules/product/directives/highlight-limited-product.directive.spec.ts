import { HighlightLimitedProductDirective } from './highlight-limited-product.directive';

describe('HighlightLimitedProductDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightLimitedProductDirective();
    expect(directive).toBeTruthy();
  });
});
